type Post {
  id: ID!,
  title: String!
  body: String
  userId: ID!
}

type Query {
  getPosts: [Post]
}

input PostInput {
  title: String!
  body: String
  userId: ID!
}

type Mutation {
  addPost(post: PostInput): Post
}